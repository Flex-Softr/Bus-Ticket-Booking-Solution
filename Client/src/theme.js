// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#143f40', // Customize the primary color
    },
    secondary: {
      main: '#1976d2', // Customize the secondary color
    },
    // Add more color customization as needed
  },
});

export default theme;
